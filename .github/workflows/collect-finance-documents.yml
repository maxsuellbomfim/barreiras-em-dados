name: Coletar finanças e documentos de pessoal

on:
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:
    inputs:
      limit:
        description: "Máximo de registros por página de cada fonte"
        required: false
        default: "50"
        type: choice
        options:
          - "10"
          - "25"
          - "50"
          - "500"
          - "100"
      max_pages:
        description: "Quantidade de páginas por fonte"
        required: false
        default: "3"
        type: choice
        options:
          - "1"
          - "3"
          - "5"
          - "20"
          - "50"
      resource:
        description: "Recurso a coletar (all executa todos)"
        required: false
        default: "all"
        type: choice
        options:
          - "all"
          - "transferegov-only"
          - "siconfi-only"
          - "bahia-state-only"
          - "bahia-special-only"
          - "tcm-ba-only"
          - "balancetes"
          - "pdc-contas-anuais"
          - "pdc-receita-tributaria"
          - "pdc-recursos-extraordinarios"
          - "pdc-resumo-execucao-da-receita"
          - "pdc-resumo-execucao-da-despesa"
          - "pdc-transferencia"
          - "pdc-emendas-parlamentares-receitas"
          - "pdc-convenios-transferencias-realizadas"
          - "pdc-obras-pdc"
          - "contratos"
          - "processos"
          - "rreo"
          - "rgf"
          - "servidores"
      personnel_reference_month:
        description: "Competência exata da folha a preservar (AAAA-MM)"
        required: false
        default: ""
        type: string
      finance_reference_month:
        description: "Competência exata do documento financeiro a preservar (AAAA-MM)"
        required: false
        default: ""
        type: string
      include_transferegov:
        description: "Preservar também propostas e parcerias destinadas a Barreiras"
        required: false
        default: true
        type: boolean
      include_transferegov_historical_proposals:
        description: "Baixar o ZIP histórico nacional e recortar Barreiras desde 2021"
        required: false
        default: false
        type: boolean
      include_transferegov_historical_amendments:
        description: "Cruzar o ZIP oficial de emendas com propostas preservadas de Barreiras"
        required: false
        default: false
        type: boolean
      include_siconfi_dca:
        description: "Preservar as contas anuais oficiais de Barreiras desde 2021"
        required: false
        default: true
        type: boolean
      include_tcm_ba_monthly:
        description: "Preservar catálogo mensal do TCM-BA no intervalo informado"
        required: false
        default: false
        type: boolean
      tcm_month_from:
        description: "Primeira competência TCM-BA (AAAA-MM)"
        required: false
        default: "2023-04"
        type: string
      tcm_month_to:
        description: "Última competência TCM-BA (AAAA-MM)"
        required: false
        default: "2023-04"
        type: string
      include_bahia_state_amendments:
        description: "Preservar o catálogo e o ZIP oficial de emendas estaduais da Bahia"
        required: false
        default: true
        type: boolean
      include_bahia_special_transfers:
        description: "Preservar o ZIP oficial de Transferências Especiais da Bahia"
        required: false
        default: true
        type: boolean
      include_bahia_state_loa_amendments:
        description: "Preservar anexos anuais da LOA da Bahia com município e autor"
        required: false
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: municipal-finance-collection-production
  cancel-in-progress: false

jobs:
  collect:
    if: >-
      github.event_name == 'schedule' ||
      (
        inputs.resource != 'transferegov-only' &&
        inputs.resource != 'siconfi-only' &&
        inputs.resource != 'bahia-state-only' &&
        inputs.resource != 'bahia-special-only' &&
        inputs.resource != 'tcm-ba-only'
      )
    name: Preservar catálogo municipal (${{ matrix.resource }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      # O plano gratuito limita as conexões simultâneas da role de coleta.
      # Oito jobs em paralelo faziam todos chegarem ao banco ao mesmo tempo
      # e produziam "too many connections" antes de persistir o documento.
      # Em execuÃ§Ã£o direcionada, somente um job coleta; os demais podem
      # verificar a condiÃ§Ã£o em paralelo. No agendamento completo, mantemos
      # uma conexÃ£o de coleta por vez para respeitar o plano gratuito.
      max-parallel: ${{ (inputs.resource == 'all' || inputs.resource == '') && 1 || 8 }}
      matrix:
        # GitHub does not expose `matrix` inside jobs.<job>.if. Build the
        # matrix from the dispatch input instead, so a directed run creates
        # exactly one job while schedules still cover every resource.
        resource: >-
          ${{ fromJSON(
            (inputs.resource == '' || inputs.resource == 'all')
              && '["balancetes","pdc-contas-anuais","pdc-receita-tributaria","pdc-recursos-extraordinarios","pdc-resumo-execucao-da-receita","pdc-resumo-execucao-da-despesa","pdc-transferencia","pdc-emendas-parlamentares-receitas","pdc-convenios-transferencias-realizadas","pdc-obras-pdc","contratos","processos","rreo","rgf","servidores"]'
              || format('["{0}"]', inputs.resource)
          ) }}
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src:workers/document-processing/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
      MUNICIPAL_TRANSPARENCY_REQUESTS_PER_MINUTE: "10"
      MUNICIPAL_TRANSPARENCY_MAX_BODY_BYTES: "16777216"
      MUNICIPAL_TRANSPARENCY_DOCUMENT_REQUESTS_PER_MINUTE: "6"
      # RelatÃ³rios fiscais podem exceder 64 MiB; o limite continua explÃ­cito
      # e abaixo do mÃ¡ximo defensivo do coletor (256 MiB).
      MUNICIPAL_TRANSPARENCY_MAX_DOCUMENT_BYTES: "268435456"
      # Limite agregado por execução: controla crescimento do Storage sem
      # impedir progresso quando um único PDF oficial ultrapassa 64 MiB.
      MUNICIPAL_TRANSPARENCY_MAX_BATCH_DOCUMENT_BYTES: "67108864"
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage]"

      - name: Verificar CA oficial do Supabase
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar catálogo municipal
        env:
          COLLECT_LIMIT: >-
            ${{ (matrix.resource == 'balancetes' || matrix.resource == 'servidores') && '500' || inputs.limit || '50' }}
          COVERAGE_ARGUMENTS: >-
            ${{ matrix.resource == 'balancetes' && '--coverage-year-from 2021' || '' }}
        run: >-
          python -B -m barreiras_collectors.commands.collect_municipal_transparency
          --source prefeitura
          --resource "${{ matrix.resource }}"
          --limit "$COLLECT_LIMIT"
          --max-pages "${{ inputs.max_pages || '3' }}"
          --allow-partial
          $COVERAGE_ARGUMENTS

      - name: Drenar documentos municipais
        # Processos licitatórios são linhas estruturadas sem URL de PDF; o
        # download de documentos não se aplica e o comando o recusaria.
        if: matrix.resource != 'processos'
        env:
          DRAIN_LIMIT: >-
            ${{ (matrix.resource == 'balancetes' || matrix.resource == 'servidores') && '500' || inputs.limit || '50' }}
          DRAIN_MAX_DOCUMENTS: >-
            ${{ (matrix.resource == 'balancetes' || matrix.resource == 'servidores') && '5' || inputs.limit || '50' }}
          DRAIN_MAX_PAGES: >-
            ${{ (matrix.resource == 'balancetes' || matrix.resource == 'servidores') && '1' || inputs.max_pages || '3' }}
          INPUT_PERSONNEL_REFERENCE_MONTH: ${{ inputs.personnel_reference_month }}
          INPUT_FINANCE_REFERENCE_MONTH: ${{ inputs.finance_reference_month }}
        shell: bash
        run: |
          extra_args=()
          if [[ -n "${INPUT_PERSONNEL_REFERENCE_MONTH:-}" && -n "${INPUT_FINANCE_REFERENCE_MONTH:-}" ]]; then
            echo "competencias exatas de pessoal e financas sao mutuamente exclusivas" >&2
            exit 2
          fi
          if [[ -n "${INPUT_PERSONNEL_REFERENCE_MONTH:-}" ]]; then
            if [[ "${{ matrix.resource }}" != "servidores" ]]; then
              echo "personnel_reference_month exige resource=servidores" >&2
              exit 2
            fi
            if [[ ! "$INPUT_PERSONNEL_REFERENCE_MONTH" =~ ^20[2-9][0-9]-(0[1-9]|1[0-2])$ ]]; then
              echo "personnel_reference_month exige AAAA-MM desde 2020" >&2
              exit 2
            fi
            extra_args+=(
              --document-reference-month "$INPUT_PERSONNEL_REFERENCE_MONTH"
              --document-type "1"
              --document-title "Relação de Servidores"
              --document-title "Relação Servidores"
              --document-title "Relação de Servidores 13º Salário"
              --allow-untyped-document-title "Relação de Servidores"
              --require-document-match
            )
          fi
          if [[ -n "${INPUT_FINANCE_REFERENCE_MONTH:-}" ]]; then
            if [[ "${{ matrix.resource }}" == "servidores" || "${{ matrix.resource }}" == "processos" ]]; then
              echo "finance_reference_month exige recurso financeiro documental" >&2
              exit 2
            fi
            if [[ ! "$INPUT_FINANCE_REFERENCE_MONTH" =~ ^20[2-9][0-9]-(0[1-9]|1[0-2])$ ]]; then
              echo "finance_reference_month exige AAAA-MM desde 2020" >&2
              exit 2
            fi
            extra_args+=(
              --document-reference-month "$INPUT_FINANCE_REFERENCE_MONTH"
              --require-document-match
            )
          fi
          python -B -m barreiras_collectors.commands.collect_municipal_transparency \
            --source prefeitura \
            --resource "${{ matrix.resource }}" \
            --limit "$DRAIN_LIMIT" \
            --max-pages "$DRAIN_MAX_PAGES" \
            --max-documents "$DRAIN_MAX_DOCUMENTS" \
            --download-documents \
            --allow-partial \
            "${extra_args[@]}"

      - name: Processar texto dos DOCX municipais
        if: matrix.resource == 'pdc-contas-anuais'
        run: >-
          python -B -m barreiras_docproc.commands.process_municipal_docx
          --limit 10
          --minimum-total 4

  transferegov:
    name: Preservar emendas federais e parcerias
    needs: collect
    if: >-
      always() &&
      inputs.resource != 'bahia-state-only' &&
      inputs.resource != 'bahia-special-only' &&
      inputs.resource != 'siconfi-only' &&
      inputs.resource != 'tcm-ba-only' &&
      (
        github.event_name == 'schedule' ||
        inputs.resource == 'transferegov-only' ||
        (
          inputs.resource == 'all' &&
          inputs.include_transferegov == true
        )
      )
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Preparar Node para o gate público
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22.x

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage]"

      - name: Verificar CA oficial do Supabase
        id: federal_preflight
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar catálogo histórico do Transferegov
        if: >-
          github.event_name != 'workflow_dispatch' ||
          inputs.resource != 'transferegov-only' ||
          inputs.include_transferegov_historical_proposals == true ||
          inputs.include_transferegov_historical_amendments == true
        run: >-
          python -B -m
          barreiras_collectors.commands.collect_transferegov_download_catalog

      - name: Preservar propostas históricas de Barreiras
        if: github.event_name == 'workflow_dispatch' && inputs.include_transferegov_historical_proposals == true
        run: >-
          python -B -m
          barreiras_collectors.commands.collect_transferegov_historical_proposals
          --year-from "2021"

      - name: Preservar emendas históricas ligadas a Barreiras
        if: github.event_name == 'workflow_dispatch' && inputs.include_transferegov_historical_amendments == true
        run: >-
          python -B -m
          barreiras_collectors.commands.collect_transferegov_historical_amendments
          --year-from "2021"

      - name: Iniciar evidência da execução federal
        id: federal_evidence
        # Um bloqueio no catálogo histórico não pode suprimir fontes independentes.
        # Sem continue-on-error: a falha original continua reprovando o job.
        if: ${{ !cancelled() && steps.federal_preflight.outcome == 'success' }}
        shell: bash
        run: |
          date -u +"%Y-%m-%dT%H:%M:%SZ" > "$RUNNER_TEMP/federal-not-before.txt"
          : > "$RUNNER_TEMP/federal-collector.log"

      - name: Preservar execução federal de emendas em Barreiras
        if: ${{ !cancelled() && steps.federal_evidence.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          python -B -m \
            barreiras_collectors.commands.collect_cgu_federal_amendments \
            2>&1 | tee -a "$RUNNER_TEMP/federal-collector.log"

      - name: Preservar documentos anuais das emendas federais
        if: ${{ !cancelled() && steps.federal_evidence.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          python -B -m \
            barreiras_collectors.commands.collect_cgu_federal_amendment_documents \
            --year-from "2021" \
            2>&1 | tee -a "$RUNNER_TEMP/federal-collector.log"

      - name: Preservar retrato do Transferegov
        if: ${{ !cancelled() && steps.federal_evidence.outcome == 'success' }}
        shell: bash
        run: |
          set -o pipefail
          python -B -m barreiras_collectors.commands.collect_transferegov_parcerias \
            --year-from "2021" \
            --page-size "${{ inputs.limit || '50' }}" \
            2>&1 | tee -a "$RUNNER_TEMP/federal-collector.log"

      - name: Confirmar publicação das fontes federais
        if: ${{ !cancelled() && steps.federal_evidence.outcome == 'success' }}
        run: >-
          node scripts/verify-public-federal-resource-projections.mjs
          --collector-log "$RUNNER_TEMP/federal-collector.log"
          --not-before-file "$RUNNER_TEMP/federal-not-before.txt"

  siconfi_dca:
    name: Preservar contas anuais do SICONFI
    # Mantém a única conexão de coleta do plano gratuito: o job começa
    # depois do Transferegov e antes das fontes estaduais.
    needs:
      - collect
      - transferegov
    if: >-
      always() &&
      (
        github.event_name == 'schedule' ||
        inputs.resource == 'siconfi-only' ||
        (
          inputs.resource == 'all' &&
          inputs.include_siconfi_dca == true
        )
      )
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src:workers/normalization/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage]"

      - name: Verificar CA oficial do Supabase
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar DCA anual de Barreiras
        run: >-
          python -B -m
          barreiras_collectors.commands.collect_siconfi_dca
          --year-from "2021"

      - name: Materializar totais anuais literais do SICONFI
        run: >-
          python -B -m
          barreiras_normalization.commands.process_siconfi_annual_totals
          --limit "10"

  tcm_ba_monthly:
    name: Preservar catálogo mensal do TCM-BA
    needs:
      - collect
      - transferegov
      - siconfi_dca
    if: >-
      always() &&
      github.event_name == 'workflow_dispatch' &&
      (
        inputs.resource == 'tcm-ba-only' ||
        (
          inputs.resource == 'all' &&
          inputs.include_tcm_ba_monthly == true
        )
      )
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage]"

      - name: Verificar CA oficial do Supabase
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar competências mensais do e-TCM
        run: >-
          python -B -m
          barreiras_collectors.commands.collect_tcm_ba_monthly_catalog
          --month-from "${{ inputs.tcm_month_from }}"
          --month-to "${{ inputs.tcm_month_to }}"
          --requests-per-minute "30"

  bahia_state_amendments:
    name: Preservar emendas estaduais da Bahia
    # Os jobs complementares usam a mesma role PostgreSQL no plano gratuito.
    # Encadeá-los evita que Transferegov, FIPLAN e LOA abram conexões ao
    # mesmo tempo depois que a matriz municipal termina.
    needs:
      - collect
      - transferegov
      - siconfi_dca
      - tcm_ba_monthly
    if: >-
      always() &&
      (
        inputs.resource == 'bahia-state-only' ||
        (
          inputs.resource != 'transferegov-only' &&
          inputs.resource != 'siconfi-only' &&
          inputs.resource != 'bahia-special-only' &&
          (
            github.event_name == 'schedule' ||
            (
              inputs.resource == 'all' &&
              inputs.include_bahia_state_amendments == true
            )
          )
        )
      )
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src:workers/normalization/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
      BAHIA_STATE_TLS_CA_BUNDLE: config/certificates/sectigo-public-server-authentication-ov-r36-chain.pem
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Preparar Node para o gate público
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22.x

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage]"

      - name: Verificar cadeia TLS oficial da fonte estadual
        run: >-
          echo "729b16d606ab35a0ca027fce556f9b35913d4553f1f9b6e88ba331625519d333  config/certificates/sectigo-public-server-authentication-ov-r36-chain.pem"
          | sha256sum --check --strict

      - name: Verificar CA oficial do Supabase
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar catálogo e ZIP estaduais
        shell: bash
        run: |
          set -o pipefail
          python -B -m \
            barreiras_collectors.commands.collect_bahia_state_amendments \
            2>&1 | tee "$RUNNER_TEMP/bahia-state-collector.jsonl"

      - name: Normalizar execução estadual agregada
        run: >-
          python -B -m
          barreiras_normalization.commands.process_bahia_state_execution
          --limit "1"

      - name: Confirmar publicação da cobertura estadual
        run: >-
          node scripts/verify-public-state-resource-projections.mjs
          --scope state-execution
          --collector-log "$RUNNER_TEMP/bahia-state-collector.jsonl"

  bahia_special_transfers:
    name: Preservar transferências especiais da Bahia
    needs:
      - collect
      - transferegov
      - bahia_state_amendments
    if: >-
      always() &&
      (
        inputs.resource == 'bahia-special-only' ||
        inputs.resource == 'bahia-state-only' ||
        (
          inputs.resource != 'transferegov-only' &&
          inputs.resource != 'siconfi-only' &&
          (
            github.event_name == 'schedule' ||
            (
              inputs.resource == 'all' &&
              inputs.include_bahia_special_transfers == true
            )
          )
        )
      )
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src:workers/normalization/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
      BAHIA_STATE_TLS_CA_BUNDLE: config/certificates/sectigo-public-server-authentication-ov-r36-chain.pem
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Preparar Node para o gate público
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22.x

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage]"

      - name: Verificar cadeia TLS oficial da fonte estadual
        run: >-
          echo "729b16d606ab35a0ca027fce556f9b35913d4553f1f9b6e88ba331625519d333  config/certificates/sectigo-public-server-authentication-ov-r36-chain.pem"
          | sha256sum --check --strict

      - name: Verificar CA oficial do Supabase
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar catálogo e ZIP de Transferências Especiais
        shell: bash
        run: |
          set -o pipefail
          python -B -m \
            barreiras_collectors.commands.collect_bahia_special_transfers \
            2>&1 | tee "$RUNNER_TEMP/bahia-special-collector.jsonl"

      - name: Normalizar pagamentos que mencionam Barreiras
        run: >-
          python -B -m
          barreiras_normalization.commands.process_bahia_special_transfers
          --limit "1"

      - name: Reconciliar cobertura, pagamentos e ranking públicos
        run: >-
          node scripts/verify-public-state-resource-projections.mjs
          --scope special-transfers
          --collector-log "$RUNNER_TEMP/bahia-special-collector.jsonl"

  bahia_state_loa_amendments:
    name: Preservar anexos territoriais da LOA da Bahia
    needs:
      - collect
      - transferegov
      - bahia_state_amendments
      - bahia_special_transfers
    if: >-
      always() &&
      (
        inputs.resource == 'bahia-state-only' ||
        (
          inputs.resource != 'transferegov-only' &&
          inputs.resource != 'siconfi-only' &&
          inputs.resource != 'bahia-special-only' &&
          (
            github.event_name == 'schedule' ||
            (
              inputs.resource == 'all' &&
              inputs.include_bahia_state_loa_amendments == true
            )
          )
        )
      )
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      APP_ENV: production
      LOG_LEVEL: INFO
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONPATH: workers/collectors/src:workers/document-processing/src
      PERSISTENCE_MODE: postgres-supabase
      DATABASE_URL: ${{ secrets.QUERIDO_DIARIO_DATABASE_URL }}
      SUPABASE_URL: ${{ vars.QUERIDO_DIARIO_SUPABASE_URL }}
      SUPABASE_PUBLISHABLE_KEY: ${{ vars.QUERIDO_DIARIO_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_WORKLOAD_EMAIL: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_EMAIL }}
      SUPABASE_WORKLOAD_PASSWORD: ${{ secrets.MUNICIPAL_TRANSPARENCY_SUPABASE_WORKLOAD_PASSWORD }}
      SUPABASE_RAW_ARTIFACTS_BUCKET: raw-artifacts
    steps:
      - name: Baixar código
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Preparar Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
          cache: pip

      - name: Preparar Node para o gate público
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22.x

      - name: Instalar dependências fixadas
        run: python -m pip install --disable-pip-version-check ".[postgres,storage,pdf]"

      - name: Verificar CA oficial do Supabase
        run: >-
          echo "700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7  config/certificates/supabase-prod-ca-2021.crt"
          | sha256sum --check --strict

      - name: Preservar anexos da LOA por município e autor
        shell: bash
        run: |
          set -o pipefail
          date -u +"%Y-%m-%dT%H:%M:%SZ" \
            > "$RUNNER_TEMP/bahia-loa-started-at.txt"
          python -B -m \
            barreiras_collectors.commands.collect_bahia_state_loa_amendments \
            --year-from "2021" \
            --year-to "2026" \
            2>&1 | tee "$RUNNER_TEMP/bahia-loa-collector.jsonl"

      - name: Extrair valores autorizados destinados a Barreiras
        run: >-
          python -B -m
          barreiras_docproc.commands.process_bahia_state_loa
          --limit "10"

      - name: Reconciliar cobertura pública da LOA e da execução
        run: >-
          node scripts/verify-public-state-resource-projections.mjs
          --scope loa
          --collector-log "$RUNNER_TEMP/bahia-loa-collector.jsonl"
          --not-before-file "$RUNNER_TEMP/bahia-loa-started-at.txt"
